{
  "name": "cinder-run-sky-relay",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "python3 -m http.server 8765 --bind 127.0.0.1",
    "test": "node --test"
  }
}
